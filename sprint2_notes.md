# Sprint 2 — 학습 루프 (선형회귀)
 
`y = 3x + 2`라는 숨겨진 규칙을, 데이터 4개만 주고 경사하강법으로 찾아내게 했다.
`w = 0`, `b = 0`에서 출발해 1000 에폭 학습.
 
## 결과
 
| 항목 | 값 |
|---|---|
| 최종 `w` | 3.00 (목표 3.0) |
| 최종 `b` | 1.95 (목표 2.0) |
| loss | 10² → 10⁻³ (5자릿수 감소) |
| 학습률 | 0.01 |
 
[학습 곡선]<img width="1600" height="571" alt="image" src="https://github.com/user-attachments/assets/47667cb6-5c1d-4425-8a70-4ba35bc97637" />

 
---
 
## 관찰 1 — 200 에폭으로는 부족했다
 
처음 200 에폭으로 돌렸을 때 수렴이 덜 됐다. 1000으로 늘려서 해결했지만,
"왜 부족했는가"가 그래프에 그대로 찍혀 있었다.
 
- `w`는 초반에 **3.27까지 오버슈트**한 뒤 600 에폭에 걸쳐 천천히 3.0으로 복귀
- `b`는 1.15에서 **느리게 기어올라** 1000 에폭에도 1.95, 목표에 미도달
### 원인 — 두 파라미터의 그래디언트 크기가 다르다
 
```
dL/dw = mean( 2(wx + b − y) · x )   ← x가 곱해져 있다
dL/db = mean( 2(wx + b − y) )       ← 없다
```
 
데이터가 `x = [1, 2, 3, 4]`, 평균 2.5이므로 **`w`의 그래디언트는 `b`보다 평균 2.5배 크다.**
같은 학습률 0.01을 써도 `w`가 2.5배 큰 보폭으로 움직인다.
 
그래서 벌어지는 일:
 
1. `w`가 혼자 앞서 나감 → `b`가 아직 0 근처라, 부족한 절편을 `w`가 대신 메꿈 → **오버슈트**
2. `b`가 뒤늦게 올라옴 → `b`가 제 몫을 하기 시작하니 `w`는 다시 3.0으로 복귀
3. 결국 **`b`의 느린 속도가 전체 학습 시간을 결정**
손실 함수의 골짜기가 원형이 아니라 **길쭉한 계곡** 모양이라서 생기는 현상.
공이 한 방향으로는 빠르게, 다른 방향으로는 굼뜨게 구른다.
 
### 실무의 해법
 
- **정규화(Normalization)** — 입력의 평균 0 / 표준편차 1로 맞춰 계곡을 동그랗게 편다.
  신경망에서는 `BatchNorm`, `LayerNorm`이 이 역할.
- **Adam 옵티마이저** — 파라미터마다 학습률을 따로 조절해, 느린 파라미터에 큰 보폭을 준다.
---
 
## 생각 정리 — 이 학습에서 컴퓨터가 하는 역할은 무엇인가
 
학습 루프를 돌리며 든 질문. 처음 떠올린 답과, 확인 후 정정한 내용을 함께 남긴다.
 
### ① 반복
 
1000번의 계산을 인간은 이 속도로 할 수 없다.
 
> **정정:** 맞지만 가장 약한 답이다. 반복 속도는 계산기도 가진 컴퓨터의 일반적 특성이고,
> 딥러닝 고유의 것이 아니다.
 
### ② 자동미분
 
미분하기 어려운 loss 함수를 자동으로 미분해준다.
 
> **정정 1 — "관찰"이 아니라 "공식"이다.**
> autograd는 값을 흔들어보고 관찰하는 방식이 아니다. PyTorch는 각 연산의 미분 공식을
> 이미 전부 알고 있고(`x²`→`2x`, `sin`→`cos` …), 녹화하는 것은 미분값이 아니라 **연산 순서**다.
> 역방향으로 가며 그 공식들을 연쇄법칙으로 이어 붙일 뿐이라, 결과는 근사값이 아닌 **정확한 해석해**다.
>
> 내가 생각한 "관찰을 통한 미분"은 실제로 존재하는데, **수치미분**이 그것이다.
> ```
> f'(x) ≈ ( f(x+h) − f(x) ) / h      # h를 아주 작게
> ```
> 딥러닝에서는 쓰지 않는다. 오차가 누적되고, 파라미터가 13,000개면 함수를 13,000번 호출해야 한다.
>
> **정정 2 — "미분하기 어려운"이 아니라 "손으로 쓰기엔 방대한"이다.**
> 신경망은 곱셈·덧셈·시그모이드가 층층이 쌓인 것뿐이라 수학적으로 어려운 함수가 아니다.
> 다만 손으로 연쇄법칙을 전개하면 A4 수천 장이 된다. 난이도가 아니라 분량의 문제.
 
### ③ 효율성 — 처음에 놓쳤던, 그리고 진짜 핵심인 답
 
**같은 비용으로 모든 방향의 미분을 한 번에 구한다.**
 
- 수치미분: 파라미터 13,002개의 그래디언트 → 함수를 **13,002번** 계산
- 역전파: 순전파 1번 + 역전파 1번 → **13,002개를 전부** 획득
비용이 파라미터 개수에 비례하지 않는다는 것. **딥러닝이 성립하는 이유가 이 하나다.**
역전파가 없다면 파라미터 1조 개인 모델은 학습 1스텝에 1조 번의 계산이 필요하다.
 
| 역할 | 판정 |
|---|---|
| 반복 속도 | 맞지만 딥러닝 고유의 것이 아님 |
| 자동미분 | 맞음. 단 "관찰"이 아니라 연쇄법칙 |
| **효율성 (역전파)** | **처음에 놓쳤던 핵심** |
 
---
 
## 다음 스프린트로 넘길 질문
 
- 입력을 정규화하면 수렴이 실제로 얼마나 빨라지는가 (같은 학습률, 같은 에폭 기준)
- SGD 대신 Adam을 쓰면 `b`의 지각 문제가 사라지는가
